setwd("/Users/imiu/Desktop/Coursera/Data Scientist Certification Classes/6.Statistical Inference/8.Project/Statistical-Inference/")
options(prompt="R:StatInfer_project> ")

library(utils)
library(base)
library(gRbase)
library(graphics)
library(grDevices)
library(ggplot2)
.libPaths() # get library location
library()   # see all packages installed
search()    # see packages currently loaded
getwd()
options(width=200)

cat("\nIonel, welcome back to R! Today is", date(), "\n")
cat("\nR is ready for you!\n")
cat("\nWorking director is: ", getwd(), "\n")
cat("\nPackages currently loaded are:\n", search(), "\n")

cat("\n                    ======================================", "\n")
cat("\n                       Statistical Inference - Project" ,    "\n")
cat("\n                    ======================================", "\n")


